{
  "name": "solstone-transparency",
  "version": "0.1.0",
  "private": true,
  "description": "sol pbc trust/transparency evidence plane. Read-side v1 verifier, v2 TUF client and record verifier, the typed portal models, and the read-only HTML renderer served at trust.solstone.app by worker.ts.",
  "license": "AGPL-3.0-only",
  "type": "module",
  "bin": {
    "solstone-transparency": "./bin/solstone-transparency.ts"
  },
  "scripts": {
    "test": "bun test",
    "typecheck": "tsc --noEmit",
    "lint": "biome check src/ bin/",
    "format": "biome format --write src/ bin/"
  },
  "devDependencies": {
    "@biomejs/biome": "1.9.4",
    "@types/bun": "^1.3.0",
    "typescript": "^5.9.0"
  },
  "trustedDependencies": [
    "@biomejs/biome"
  ]
}
